# Optional. SCOPE works fully without this key.
# When set, story pages gain a short, clearly-labelled AI briefing.
# Without it, that section is simply omitted — nothing is fabricated.
ANTHROPIC_API_KEY=
